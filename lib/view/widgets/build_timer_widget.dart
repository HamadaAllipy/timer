import 'package:flutter/material.dart';

class BuildTimerWidget extends StatelessWidget {
  const BuildTimerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
